import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Preferências de acessibilidade' };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
