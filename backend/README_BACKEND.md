# Backend

API inicial em FastAPI para apoiar a evolucao da plataforma educacional.

## Executar localmente

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python main.py
```

## Rotas atuais

- `GET /`
- `GET /api/v1/health`
